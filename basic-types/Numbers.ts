/** Numbers: All numbers in TypeScript are either floating-point 
    alues that get the number type or big integers that get the bigint type */

let price: number; // floating-point
let bagPrice = 9.95;
let x: number = 100,
  y: 200;

// binary numbers
let bin = 0b100;
let anotherBin: number = 0b010;

// big integers
let big: bigint = 9007199254740991n;

export {};
