let skills: string[];
skills = ["TS"];

console.log(skills.length);

let series = [1, 2, 3];
let doubled = series.map((num) => num * 2);
console.log(doubled);

/// storing mixed types values
let scores: (string | number)[];
scores = ["Programming", 6, 'SWD', 6];

export {};
